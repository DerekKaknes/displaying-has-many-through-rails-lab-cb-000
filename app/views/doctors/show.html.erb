<h1>Doctors#show</h1>
<%= @doctor.name %>, <%= @doctor.department %>
<h2>Appointments</h2>
<% @doctor.appointments.each do |appt| %>
  <div>
  <%= link_to appt.patient.name, patient_path(appt.patient) %> | <%= appt.display_time %>
  </div>
<% end %>

